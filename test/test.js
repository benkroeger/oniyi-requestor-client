/*global describe, it */
'use strict';
var assert = require('assert');
var oniyiRequestorClient = require('../');

describe('oniyi-requestor-client node module', function () {
  it('must have at least one test', function () {
    oniyiRequestorClient();
    assert(false, 'I was too lazy to write any tests. Shame on me.');
  });
});
